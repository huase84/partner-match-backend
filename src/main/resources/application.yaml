server:
    port: 8081
spring:
    application:
        name: parthnerMatch
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://127.0.0.1:3307/partner_match?useSSL=false&serverTimezone=UTC
        username: root
        password: root

mybatis-plus:
    type-aliases-package: com.th.partnermatchcloud.entity # 别名扫描包

